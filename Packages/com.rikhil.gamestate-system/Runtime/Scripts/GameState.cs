// Game State is enum file which is created to track the state of game and show relevant panel to the user
public enum GameState
{
    Internet,
    MainMenu,
    LevelSelect,
    GamePlay,
    Pause,
    LevelComplete
}
